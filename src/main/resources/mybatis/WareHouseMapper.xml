<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bh.mall.dao.IWareHouseDAO">
	<resultMap id="wareHouse_DB_Result" type="wareHouse">
		<result column="code" property="code" />
		<result column="type" property="type" />
		<result column="currency" property="currency" />
		<result column="product_code" property="productCode" />
		<result column="product_name" property="productName" />
		
		<result column="product_specs_code" property="productSpecsCode" />
		<result column="product_specs_name" property="productSpecsName" />
		<result column="price" property="price" />
		<result column="quantity" property="quantity" />
		<result column="amount" property="amount" />
		
		<result column="user_id" property="userId" />
		<result column="status" property="status" />
		<result column="create_datetime" property="createDatetime" />	
		<result column="last_change_code" property="lastChangeCode" />
		<result column="system_code" property="systemCode" />
		<result column="company_code" property="companyCode" />
		
	</resultMap>
	<sql id="where_condition">
		<trim prefix="WHERE" prefixOverrides="AND | OR">
			<if test="code != null and code != '' ">
				AND t.code = #{code}
			</if>
			<if test="keyword != null and keyword != '' ">
				AND t.product_name like  "%"#{keyword}"%" or t.real_name  like  "%"#{keyword}"%"
			</if>
			<if test="productSpecsCode != null and productSpecsCode != '' ">
				AND t.product_specs_code = #{productSpecsCode}
			</if>
			<if test="productCode != null and productCode != '' ">
				AND t.product_code = #{productCode}
			</if>
			<if test="userId != null and userId != '' ">
				AND t.user_id = #{userId}
			</if>
			<if test="status != null and status != '' ">
				AND t.status = #{status}
			</if>
		</trim>
	</sql>
	
	<select id="select_wareHouse" parameterType="wareHouse" resultMap="wareHouse_DB_Result">
		SELECT
		t.code
		,t.type
		,t.amount
		,t.currency
		,t.product_code
		
		,t.product_name
		,t.product_specs_code
		,t.product_specs_name
		,t.price
		,t.quantity
		
		,t.user_id
		,t.status
		,t.create_datetime
		,t.last_change_code
		FROM
		tbh_ware_house t
		<include refid="where_condition" />
		<trim prefix="ORDER BY ">
			<if test="order != null and order != '' ">
				t.${order}
			</if>
		</trim>
	</select>

	<select id="select_wareHouse_count" parameterType="wareHouse"
		resultType="java.lang.Long">
		SELECT count(1) FROM tbh_ware_house t
		<include refid="where_condition" />
	</select>

	<insert id="insert_wareHouse" parameterType="wareHouse">
		INSERT INTO
		tbh_ware_house(
		code
		,type
		,currency
		,product_code
		,product_name
		
		,product_specs_code
		,product_specs_name
		,price
		,quantity
		
		,amount
		,user_id
		,status
		,create_datetime
		,last_change_code
		,system_code
		,company_code
		)
		VALUES(
		#{code}
		,#{type}
		,#{currency}
		,#{productCode}
		,#{productName}
		
		,#{productSpecsCode}
		,#{productSpecsName}
		,#{price}
		,#{quantity}
		,#{amount}
		
		,#{userId}
		,#{status}
		,#{createDatetime}
		,#{lastChangeCode}
		,#{systemCode}
		,#{companyCode}
		)
	</insert>

	<update id="update_quantity" parameterType="wareHouse">
		UPDATE tbh_ware_house
		SET
		quantity = #{quantity}
		,amount = #{amount}
		,last_change_code = #{lastChangeCode}
		WHERE code = #{code}
	</update>

	<update id="update_logCode" parameterType="wareHouse">
		UPDATE tbh_ware_house
		SET
		last_change_code = #{lastChangeCode}
		WHERE code = #{code}
	</update>


</mapper>
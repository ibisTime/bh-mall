<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bh.mall.dao.IAgentAllotDAO">
	<resultMap  id="agentAllot_DB_Result" type="agentAllot" >
		<result column="code" property="code"/>
		<result column="apply_user" property="applyUser" />
		<result column ="to_user_id" property="toUserId"/>
		<result column="apply_level" property="applyLevel" />
		<result column ="apply_datetime" property="applyDatetime"/>
		<result column ="manager" property="manager"/>
		<result column ="status" property="status"/>
		<result column="approve_datetime" property="approveDatetime" />		
		<result column="remark" property="remark"/>
	</resultMap>
	
	<sql id="where_condition">
		<trim prefix="WHERE" prefixOverrides="AND | OR">
			<if test="code != null and code != ''">
				AND t.code=#{code}
			</if>
			<if test="applyUser != null and applyUser != '' ">
				AND t.apply_user = #{applyUser}
			</if>
			
			<choose>
				
				<when test="status == '3,4,5' "> <!--  有意愿，已忽略，已分配 -->
					AND t.status = '3' OR t.status = '4' OR t.status = '5' 
				</when> 
				<otherwise>
					AND 1=1
				</otherwise>
			</choose>
			
			<if test="toUserId != null and toUserId != '' ">
				AND t.to_user_id = #{toUserId}
			</if>
			
			<if test="applyDatetimeStart != null and applyDatetimeStart != '' ">
				<![CDATA[AND t.apply_datetime >= #{applyDatetimeStart}]]>
			</if>
			<if test="applyDatetimeEnd != null and applyDatetimeEnd != '' ">
				<![CDATA[AND t.apply_datetime <= #{applyDatetimeEnd}]]>
			</if>
			<if test="approveDatetimeStart != null and approveDatetimeStart != '' ">
				<![CDATA[AND t.approve_datetime >= #{approveDatetimeStart}]]>
			</if>
			<if test="approveDatetimeEnd != null and approveDatetimeEnd != '' ">
				<![CDATA[AND t.approve_datetime <= #{approveDatetimeEnd}]]>
			</if>
			
			<if test="manager != null and manager != '' ">
				AND t.manager = #{manager}
			</if>
						
		</trim>
	</sql>
	
	<select id="select_agentAllot" parameterType="agentAllot" resultMap="agentAllot_DB_Result">
		SELECT 
		t.code 
		,t.apply_user
		,t.to_user_id
		,t.apply_level
		,t.apply_datetime
		,t.manager
		,t.status
		,t.approve_datetime
		,t.remark
		FROM tbh_agent_allot t LEFT JOIN tbh_buser u ON t.apply_user = u.user_id 
		<include refid="where_condition"/>
		<trim prefix="ORDER BY">
			<if test="order != null and order != '' ">
				${order}
			</if>
		</trim>
	</select>
	
	<select id="select_agentAllot_count" parameterType="agentAllot" resultType="java.lang.Long">
		SELECT count(1) FROM tbh_agent_allot t LEFT JOIN tbh_buser u ON t.apply_user = u.user_id 
		<include refid="where_condition"/>
	</select>
	
	<insert id="insert_agentAllot" parameterType="agentAllot">
		INSERT INTO
		tbh_agent_allot(
		code
		,to_user_id		
		,apply_user		
		,apply_level		
		,status
		,apply_datetime
		,manager
		,approve_datetime
		,remark
		)
		VALUES(
		#{code}
		,#{toUserId}
		,#{applyUser}
		,#{applyLevel}	
		,#{status}
		,#{applyDatetime}
		,#{manager}
		,#{approveDatetime}
		,#{remark}
		)
	</insert>
	
	
	
	<update id="update_agentAllot" parameterType="agentAllot">
		UPDATE tbh_agent_allot
		SET
		to_user_id=#{toUserId}
		,apply_user = #{applyUser}
		,apply_level = #{applyLevel}
		,apply_datetime=#{applyDatetime}
		,manager=#{manager}
		,status=#{status}	
		,remark=#{remark}
		WHERE code=#{code}
	</update>
	
<!-- 意向分配已完成 -->



</mapper>